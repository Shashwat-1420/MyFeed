# AGENTS.md — MyFeed / SavedFeed

Guidance for AI agents (and humans) working in this repo. Read this before changing code.

---

## 1. What this project is

A "save-it-now, resurface-it-later" read-later / second-brain app: share a link from
Instagram / YouTube / Reddit / X, it gets scraped, AI-categorized, and then resurfaced on a
spaced-repetition schedule so you actually retain what you save.

It is an entry for the **iQOO hackathon** (Reskilll platform), with two deadlines that drive
all sequencing:

| Date | Event | Notes |
|---|---|---|
| **2026-09-22** | Reskilll platform submission | Web app (Phase A) pushed ✅ |
| **2026-09-26/27** | Hyderabad City Battle demo | Loaner iQOO phone handed at check-in — sideload dev APK there |
| **2026-10-09/11** | Finale (per original plan) | |

The iQOO judging rubric: **End product 30% · Novelty 20% · Creative phone use 15% ·
Technical depth 15% · Office Kit 10% · Demo 10%**. It explicitly rewards **a local or
open-source model at the core** and **on-device / NPU usage**.

**Implication: on-device local AI categorization is the single highest-value feature for scoring.**

---

## 2. Current state of the repo

- **Stack:** **Expo SDK 57 + React Native 0.86 + React 19.2 + NativeWind 4 + Zustand**.
  The old Vite web app has been **replaced** (Phase B). It still exists in git history — the last
  commit before the Expo conversion.
- **Entry:** `index.ts` → `src/App.tsx`. Views in `src/views/`, components in
  `src/components/{common,navigation,saves}/`.
- **State:** `src/store/useSavedFeedStore.ts` (Zustand) is the single source of truth.
  Store-driven navigation — **no router**; `currentTab` + `currentScreen` decide what renders.
- **Styling:** NativeWind (Tailwind classes on RN). Semantic tokens only — see §5.
- **Native config:** `app.json` drives `expo prebuild` (CNG). `android/` and `ios/` are
  **generated, gitignored, never committed**.
- **Backend:** `supabase/` holds `schema.sql` and an edge function `process-save`. It is
  optional/legacy — on-device AI is the primary path and `aiAdapter.ts` keeps a keyword fallback.

### Commands
```bash
npm install                    # install deps
npx tsc --noEmit               # typecheck — primary gate (currently 0 errors)
npx expo export -p android     # bundle gate — catches NativeWind/babel/Metro errors
npx expo start                 # dev server (QR / dev-client)
npx expo run:android --device  # build + install on a USB phone (auto-prebuilds)
npx expo prebuild -p android --clean   # regenerate android/ from app.json
npm run lint                   # BROKEN — eslint not installed, no config. Not a regression signal.
```
`npm run build` (Vite) **no longer exists** — it was removed with the web app.

### Building the release APK (the demo artifact)
```bash
cd android && ./gradlew assembleRelease
# -> android/app/build/outputs/apk/release/app-release.apk  (~166 MB)
```
- The JS bundle is **embedded**, so it runs **standalone** — no Metro, no laptop. This is what gets
  sideloaded onto the loaner iQOO.
- It is signed with the **debug keystore** (Expo's generated Gradle config), which is fine for
  sideloading but **not Play-Store-ready** (that needs a real release keystore / EAS).
- Install over an existing build with `adb install -r <apk>` (same signature, so it upgrades in
  place and keeps the downloaded model).
- Builds are slow (~17 min, and ~1 h the first time due to the NDK + ExecuTorch C++ compile).

### Known repo quirks
- `npm run lint` is broken. Don't "fix" it by installing eslint unless asked.
- `android/`/`ios/` are regenerated by prebuild; edit `app.json` (or config plugins), not the
  generated native files.
- The old planning docs (`savedfeed_final_implementation_plan.md`,
  `savedfeed_design_report_antigravity.md`, `savedfeed_project_context (1).md`) were **deleted** —
  they were stale; the source is the only truth now.
- Supabase is **optional** and unconfigured by default. To enable it, create a `.env` containing
  `EXPO_PUBLIC_SUPABASE_URL` and `EXPO_PUBLIC_SUPABASE_ANON_KEY` (`.env` is gitignored; there is no
  committed example file). The app runs fully offline without it — the on-device model plus the
  keyword fallback need no backend.

---

## 3. The agreed roadmap (Phases A → B → C)

### Phase A — pre-submission web fixes — ✅ DONE (2026-09-22)

Fixed dark mode by moving chrome to **CSS-variable-backed semantic Tailwind tokens**
(`bg-canvas`/`bg-panel`/`bg-chip`/`text-ink`/`text-muted`/`text-dim`/`border-edge`/`text-gold`),
swept the leftover indigo palette to iQOO yellow, fixed white-on-gold contrast, added the
missing `fadeIn`/`bounceIn` keyframes, and documented the roadmap here. Verified: `tsc` → 0,
`vite build` → 0.

### Phase B — Expo conversion — ✅ DONE (2026-09-22)

The Vite app was replaced by an Expo/RN app **in the same repo**:

- **Expo SDK 57** (not the 54/55 in the old plan — SDK 57 is current and `expo-share-intent@8`
  requires `expo ^57`). RN 0.86.3, React 19.2.3.
- **NativeWind 4** set up: `babel.config.js` (`jsxImportSource: nativewind` + `nativewind/babel`),
  `metro.config.js` (`withNativeWind(config, { input: './global.css' })`),
  `tailwind.config.js` (`presets: [require('nativewind/preset')]`), `global.css`,
  `nativewind-env.d.ts`.
- **Theme ported via `vars()`** (`src/lib/theme.ts`) instead of `:root` CSS, applied on the root
  `<View>` in `App.tsx` from the store's `darkMode`. `useThemeColors()` exposes resolved hexes for
  places className can't reach (react-native-svg icon `color`/`fill` props).
- **Brand yellow is now `#FFC800`** (was `#F0B31C`), on near-black `#08080C`. Light-mode gold
  text darkens to `#8A6A00` for contrast.
- **All 9 views + 9 components ported**: `div→View`, text tags→`Text`, `button→Pressable`,
  `input/textarea→TextInput`, `img→Image`, `lucide-react→lucide-react-native`.
  `MobileFrame` was dropped (web-only chrome).
- **Porting decisions for web-only features:**
  - `canvas-confetti` → no-op shim `src/lib/confetti.ts` (`celebrate()`) — see Phase C polish.
  - CSS gradients (`bg-gradient-to-*`) → flat translucent scrims/tints.
  - `@keyframes` animations, `backdrop-blur`, `hover:`/`group-hover:` → dropped (no RN equivalent).
  - `space-x-*`/`divide-*`/`grid-cols-*` → `gap-*`, explicit borders, `flex-wrap`.
  - `overflow-*` → `ScrollView`; `fixed` → absolute overlay / RN `Modal` (BottomSheetModal).
  - `<select>` → inline chip picker.
  - Profile JSON export: Blob/`<a download>` → RN `Share` sheet.
  - `window.open`/`<a href>` → `Linking.openURL`.
  - `document.*` in the store → removed (theme is applied by the root View).
  - `import.meta.env` → `process.env.EXPO_PUBLIC_*`; `new URL()` → regex host parse (Hermes-safe).
- **Verified:** `npx tsc --noEmit` → 0 errors; `npx expo export -p android` → bundled (3031 modules);
  `npx expo prebuild -p android` → succeeded and applied the share-intent filters.

### Phase C — differentiating features, in value order

1. **Share-to-app** — ✅ DONE. `expo-share-intent` wired via `useShareIntent()` in `App.tsx` into
   the `sharedUrlPayload` → `new_save` flow (URL de-duped with a cooldown, intent always
   acknowledged, and the sharing app's own `meta.title` passed through as a hint). **Real link
   previews** replaced the old placeholder mock: `src/lib/scraper.ts` now does og:/twitter: card
   scraping (RN `fetch` has no CORS), with **YouTube oEmbed** for accurate video titles +
   thumbnails, and a slug-derived title fallback. Verified on device: a YouTube share produced the
   real video title, channel and thumbnail.
   - **Known limitation:** Instagram and X block non-browser fetches, so those fall back to the
     slug title and a monochrome placeholder thumbnail. No client-side scraper can beat that.
2. **On-device AI categorization** — ✅ DONE. `react-native-executorch` + **Qwen2.5-0.5B-Instruct
   (8da4w)** running on the **XNNPACK CPU backend**. See "On-device AI" below for the details,
   constraints and gotchas. Verified on device: a shared GitHub link was categorized *on-device*
   and the UI reports **"Categorized on-device · local model, offline"**.
3. **Anki-style spaced-repetition notifications** — ✅ DONE. `src/lib/resurface.ts` now implements
   **SM-2** (`sm2Update`, ease factor floored at 1.3, intervals clamped to 365d) instead of the
   fixed ladder, and `src/lib/notifications.ts` schedules a **real local notification** (no push
   server, fires with the app closed) at the user's nudge time. Verified on device: reviewing a
   save took it from 1→2 repetitions, 3→6 days, ease 2.50→2.60, and the reminder appeared in the
   Android notification shade.
   - `SaveItem` gained optional `easiness` / `interval_days`; old saves default to 2.5 / the ladder.
   - The reminder is a **single daily notification** whose text is refreshed whenever `saves`
     changes, so the due count stays accurate.
   - `POST_NOTIFICATIONS` arrives via the library manifest — **no extra config plugin needed**.
   - Profile → **Send Test Reminder** fires one immediately (demo-friendly).
   - Profile → **Daily Nudge Time** is a real picker (hour 05–23, minute 00/15/30/45) backed by
     `setNudgeTime`; changing it re-schedules the daily notification (App.tsx watches
     `profile.daily_nudge_time`).
4. **Reading UX without WebView memory cost** — ✅ effectively satisfied. There is **no WebView**
   anywhere; the app shows the extracted title/description/tags in `SaveDetailView`, and "Open
   Original Source" uses `Linking.openURL`, which hands the URL to **Android App Links** — so it
   opens the native YouTube/Instagram/Reddit app when installed and the browser otherwise. That is
   the required "deep-link to the platform app, fall back to browser" behaviour with no extra
   dependency. (A true Chrome Custom Tab would need `expo-web-browser` + a rebuild, and is a worse
   experience than the native app.)
5. **Stretch: usage-stats-driven suggestions** — only if time permits. Needs `PACKAGE_USAGE_STATS`
   (special-access permission granted in Settings) plus a native rebuild; highest risk, lowest value.

### On-device AI (Phase C #2) — how it works

- **Library:** `react-native-executorch@0.10.2` (needs New Architecture, worklets ≥0.10, and
  **`minSdkVersion` 26** via `expo-build-properties`). Peer native modules:
  `react-native-blob-util`, `@kesha-antonov/react-native-background-downloader`.
- **Important:** its npm **postinstall is blocked by npm by default** and downloads the prebuilt
  ExecuTorch `.so` libs into `third-party/`. It is approved in `package.json` → `allowScripts`
  (as `react-native-executorch@0.10.2`). If those libs are missing, the native build fails.
- **Model:** `models.llm.QWEN2_5_0_5B.DEFAULT` — **~417 MB** `.pte`, downloaded on **first launch**
  (into `Android/data/com.savedfeed.app/files/react-native-executorch/`). After that it is fully
  offline. It loads into memory at app start (takes a few seconds).
- **Wiring:** `src/lib/localModel.ts` holds the session at module scope so the non-React
  `aiAdapter.processSaveWithAI` can use it; `App.tsx` mounts `useLocalModel()` once and publishes
  readiness/progress to the store.
- **Fallback chain in `aiAdapter.ts`:** on-device model → optional Supabase edge function →
  **keyword classifier** (kept, so the app always works — including before the model is ready).
  The result carries `engine: 'on-device' | 'edge' | 'keywords'`, surfaced in the New Save UI.
- **Prompting a 0.5B model is the hard part.** A bare "return JSON" instruction is unreliable; it
  needs a **system prompt + a one-shot example + a trailing `JSON:` cue**, and even then parsing
  must be **tolerant** (strict JSON first, then scan for a known category id/label). Tags from a
  model this size can be slightly off — acceptable, and the category is what matters.
- **NPU:** still CPU-only (see §4). XNNPACK on Android; Vulkan is linked but the registry resolves
  this model to XNNPACK. Qualcomm/MediaTek NPU offload remains a stretch, not a deliverable.

### Architecture decisions already made (do not re-litigate)

- **Dev build (not stock Expo Go)** — Expo Go cannot receive share intents, run local models, or
  read usage stats. (`useShareIntent({ disabled: true })` is the escape hatch for Expo Go smoke tests.)
- **Expo + React Native + NativeWind.**
- **Replace** the web app rather than keeping both (done).
- **Local model via `react-native-executorch` or `llama.rn`**; keyword fallback retained.
- **SM-2** upgrade of `resurface.ts` for notifications.
- **No persistent WebView**; deep link → Custom Tab fallback.

---

## 4. Environment / setup notes

- **Android SDK: installed headless** at `~/android-sdk` — `cmdline-tools/latest`, `platform-tools`
  (adb 37.0.1), `platforms;android-35` + `android-36`, `build-tools;35.0.0` + `36.0.0`, licenses
  accepted. `ANDROID_HOME` / `ANDROID_SDK_ROOT` / PATH are exported in `~/.zshrc`. Java 21, Node present.
- **Phone with USB debugging must be connected** for `expo run:android --device`
  (`adb devices` must list it, and the computer must be authorised on-device).
- **Test device: Nothing CMF Phone 1 — MediaTek Dimensity 7300, NOT Snapdragon.**
  **NPU reality check:** these RN local-model runtimes expose CPU (and sometimes GPU) inference;
  Qualcomm Hexagon NPU offload needs the QNN backend and a compatible build. On the CMF Phone 1 the
  realistic target is **CPU inference with a small quantized model** — still fully on-device, which
  is what the rubric rewards. Treat NPU as a stretch (and note the loaner iQOO at Hyderabad *is*
  Snapdragon, so NPU could be revisited there).
- The demo phone is a **loaner iQOO handed at Hyderabad check-in (Sep 26–27)** — sideload the
  **release APK** there (`android/app/build/outputs/apk/release/app-release.apk`); it needs no
  laptop or Metro. Don't plan on the reviewer having this repo.
- **First launch needs network:** the on-device model (~417 MB) downloads on first run. After that
  the app is fully offline.
- EAS account / cloud-build path: **unknown / unused** — local builds via Gradle / `expo run:android`.
- ⚠️ **`app.json` changes only take effect after a re-prebuild.** `minSdkVersion` is declared as 26
  in `app.json` (an ExecuTorch requirement) but the generated `android/` still reports 24, because
  it was not regenerated after that edit. Harmless on the target device (API 36) — re-run
  `npx expo prebuild -p android --clean` and rebuild if you need them in sync.
- **Git identity is repo-local and deliberate:** `Gishnu Gompa
  <75305540+sproutcake23@users.noreply.github.com>`. The numeric ID must be the full `75305540`; a
  truncated `5305540` silently mis-attributes commits to an unrelated GitHub account. Verify with
  `gh api user --jq '.login, .id'` before changing it.

---

## 5. House rules for agents

- **Scope:** do exactly what was asked (a phase, a fix). Don't refactor unrelated code.
- **Match surrounding style.** Store-driven, mock-first, NativeWind-classed.
- **Verify before claiming done:** `npx tsc --noEmit` **and** `npx expo export -p android` must both
  pass. Do not rely on `npm run lint` (broken, see above).
- **Theme:** use the semantic tokens (`bg-canvas`/`bg-panel`/`bg-chip`/`text-ink`/`text-muted`/
  `text-dim`/`border-edge`/`text-gold`) for chrome, never raw hex. Raw hex is allowed only for:
  the brand yellow fill (`bg-gold-fill` + `text-black`), the destructive red (`#F87171` / `c.danger`),
  and the light-mode palette in `src/lib/theme.ts`.
- **Monochrome by design.** The UI is **yellow / black / white only** — do not introduce
  per-category or per-platform colours. Categories carry a **lucide icon**, not an emoji and not a
  colour (`src/lib/categories.ts`); tinting comes from the shared gold tokens. Avoid emoji in
  general — use a lucide icon when a glyph carries meaning, otherwise drop it.
- **Thumbnails:** always render through `SaveThumbnail` (`src/components/common/SaveThumbnail.tsx`)
  so a missing `og:image` falls back to a gold-on-panel domain initial, never a stock photo.
- **Both themes matter.** Any new surface/text colour must work in dark *and* light — add values to
  both `DARK_VARS` and `LIGHT_VARS` in `src/lib/theme.ts` (and `THEME` for icon hexes).
- **Native constraints to remember:** no CSS gradients, no `backdrop-blur`, no `hover:`, no CSS
  `@keyframes`, no CSS grid, no `divide-*`/`space-x-*` (use `gap-*`), no `fixed` — see the Phase B
  list for the RN equivalents already in use.
- **🚨 NEVER change a `className` string at runtime.** On this stack (NativeWind 4.2.7 + RN 0.86 +
  New Architecture) a className that varies between renders sends the JS thread into a permanent
  ~130% CPU spin and leaks memory (~600 MB/minute), which eventually ANRs the app. This is *not* a
  React render loop — no re-renders are logged; the JS thread just burns. Keep `className` constant
  and put anything dynamic in the `style` prop:
  `style={{ backgroundColor: isActive ? '#FFC800' : c.panel }}`. `active:` variants are safe.
  Verified after the fix: idle 11% CPU → 19–23% through share/tab/chip/mode interactions, memory
  flat at ~560 MB.
- **Icons:** `lucide-react-native` — pass `size`/`color`/`fill` props, not `className` (SVG props
  aren't reached by Tailwind). Use `useThemeColors()` for the colour.
- **The source is the only truth.** The old planning docs were deleted — don't reintroduce them.

---

## 6. Known gaps & roadmap (internal — deliberately NOT in the public README)

Stated plainly so we never overclaim to judges:

- **Inference is CPU, not NPU.** `react-native-executorch` runs Qwen2.5-0.5B on **XNNPACK**. Qualcomm
  Hexagon / MediaTek APU offload needs a vendor backend (QNN) plus a compatible export — a stretch,
  not a deliverable. The loaner iQOO *is* Snapdragon, so a time-boxed spike there is worthwhile.
- **Instagram and X previews don't work.** Both block non-browser fetches, so those shares fall back
  to a slug-derived title + monochrome placeholder. YouTube / Reddit / GitHub / blogs / docs work.
- **Search is keyword-based, not semantic.** On-device embeddings (ExecuTorch ships `useTextEmbedder`
  with `all-MiniLM-L6-v2`) would make it semantic and replace the old pgvector plan.
- **No camera or voice features.** The rubric's "creative phone use" (15%) rewards them; we only
  claim the on-device-AI portion.
- **Office Kit is unused** — 10% of the rubric is HackTracker phone↔laptop bridge time.
- **No social layer.** Sharing saves with friends was in the original concept, never built.

Roadmap, in value order: on-device embeddings for semantic search → SM-2 interval tuning per content
type → per-platform deep links → social layer.
